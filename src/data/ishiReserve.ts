/** パワーストーンの予約販売「岡のお見立て」の設定。★2026-08-08 新設
 *
 *  ★何が変わったか（2026-08-08 岡さん・福田さん判断）
 *    それまでは「一点ものを1石ずつ並べて、お客さまが選んで買う」形でした。
 *    これを **予約販売** に変えます。
 *      ・お渡しできるのは、竹ざるに並べた数だけ（1回あたり数点）
 *      ・お客さまは石を選びません。申込時の言葉から、**岡が一つ選んで**お送りします
 *      ・そのため 1石＝1ページ（/mind/0012/ など）は不要になりました
 *
 *  🔴 このファイルだけで運用できるようにしてあります。
 *     残数が減ったとき、売り切れたとき、次の回を出すとき、**ページのコードは触らないでください。**
 *
 *  🔴 表示は /mind/ の⑥セクション1箇所だけです。 */

/** サービス名。★2026-08-08 命名（福田さん決定）
 *
 *  🔴 **名前にモノ（石・パワーストーン）を入れていません。** これは意図的です。
 *     岡さんは石以外にもその人に合うものを選んでいます。実際このプロジェクトでは
 *     0013＝大王松の松ぼっくりを扱い、2026-08-06 に一度取り下げた経緯があります
 *     （content.config.ts の kind enum に今も残っています）。
 *     名前に「石」を入れると、次に石以外を出すときに
 *     「名前を変える」か「名前と中身がずれたまま使う」かの二択になります。
 *     ⚠️ 「見立て石」「お見立てパワーストーン」等に**変えないこと。**
 *
 *  🔴 「見立て」は、呉服屋や眼鏡屋の「お見立て」です。
 *     **岡さんがその人に合うものを選ぶ**という意味が、説明なしで伝わります。
 *     ⚠️ 商品ファイルにあった「## 岡の見立て」（岡さん本人の見立てを書く欄・ヒアリング待ち）
 *        とは別物です。あちらは石1つについての所見。混同しないこと。 */
export const SERVICE_NAME = '岡のお見立て';

/** Vol番号の表記をつくる。round: 1 → 「Vol.01」
 *  🔴 表記を変えたくなったら**ここだけ**直せば、ページも履歴も全部変わります。
 *     ページ側に 'Vol.' と直接書かないこと。 */
export const volNo = (n: number) => `Vol.${String(n).padStart(2, '0')}`;

/** ── 回（Vol）の一覧。★2026-08-08 新設 ──
 *
 *  🔴 **いちばん後ろが「今回」です。** それより前は自動的に「これまでのお見立て」に回ります。
 *     次の回を出すときは、この配列の**末尾に1つ足すだけ**。前のものは消しません。
 *
 *  🔴 写真は**上書きせず、連番で増やします**（vol01.jpg → vol02.jpg → …）。
 *     履歴を写真付きで残すと決めたので（2026-08-08 福田さん判断）、
 *     上書きすると過去の回の写真が消えます。**絶対に上書きしないこと。**
 *
 *  ⚠️ w / h は書き出した画像の**実寸**です。適当な数字を書くとレイアウトが跳ねます。
 *     `sips -g pixelWidth -g pixelHeight ファイル名` で確認できます。 */
export const volumes = [
  {
    vol: 1,
    photo: '/assets/ishi/vol01.jpg',
    alt: '竹ざるの上に放射状に並べた9つの天然石。紫、青、透明、金色。色も形もひとつずつ違う',
    /** 何点お渡しできるか。写真に写っている数と必ず合わせること */
    count: 9,
    /** 履歴に出す時期。「2026年8月」のように月まで */
    released: '2026年8月',
    w: 1600,
    h: 1200,
  },
] as const;

/** 今回の回。＝ volumes のいちばん後ろ */
export const currentVolume = volumes[volumes.length - 1];

/** これまでの回。＝ 今回より前のぶん。0件のあいだ、ページには何も出ません */
export const pastVolumes = volumes.slice(0, -1);

export const ishiReserve = {
  /** 今回が何回目か。★ volumes の末尾の vol と**必ず同じ数字**にすること。
   *  ⚠️ ここだけ上げて volumes に足し忘れると、Vol.02 と表示されているのに
   *     写真は Vol.01 のまま、という状態になります。 */
  round: currentVolume.vol,

  /* 🔴 2026-08-08：残数（remaining）は**持たないことにしました**（福田さん判断）。
        在庫はStripeの「購入回数の上限」で管理します。サイトにも数字を出しません。
        理由：スプレッドシートとStripeとこのファイルの3箇所で同じ数を持つと、必ずずれます。
        ⚠️ 「残りあと2つ」を出したくなったら、Stripe側と二重管理になることを承知の上で。
           煽らないページなので、そもそも出さないほうが合っています。 */

  /** 税込・送料込。特商法ページの送料規定（10,000円以上は無料）に合致します */
  price: 18000,

  /** 受付中かどうか。売り切れ・一時停止のときは false。
   *  false にするとボタンが「受付を終了しました」に変わります。
   *  🔴 **自動では切り替わりません。** Stripeの上限に達すると決済ページ側が停止しますが、
   *     サイトのボタンは押せたままです（押すと「リンクが無効」の画面に飛びます）。
   *     売り切れたら、ここを false にしてください。 */
  open: true,

  /** Stripeの決済リンク（Payment Link）のURL。
   *  🔴 空にすると、ボタンは /contact/ に落ちます（決済の代わりに問い合わせ）。
   *     受付を止めたいときは open を false にするほうが、意図が伝わります。 */
  stripeLink: 'https://buy.stripe.com/fZu6oH2RpcQc9bl0JkejK02',

  /** お届けの目安。
   *  🔴 特商法ページ（commerce-law.astro の「役務・商品の提供時期」）と**同じ日数**にすること。
   *     今は「7〜14日以内」で揃えています。片方だけ縮めないでください。 */
  delivery: 'ご入金の確認後、7〜14日以内に発送します。',
} as const;

/** 申込時に伺うこと。岡さんが「今のその人」を掴むための手がかりです。
 *
 *  🔴🔴 **この3つは福田さんの叩き台です。岡さん本人の確認がまだ取れていません。**
 *       選定の質を決める部分なので、岡さんの言葉に置き換えてください（2026-08-08）。
 *
 *  ⚠️ 3つまでにしてあります。Stripeの決済リンクに直接付けられる質問（カスタムフィールド）が
 *     **最大3つ**だからです。4つ目を足すと、決済とは別にフォームが要る＝申込が2段階になります。
 *
 *  ⚠️ ページに出すのは「何を聞かれるか」の予告です。ここで答えてもらうわけではありません。
 *     実際の入力欄は Stripe 側にあります。**文言を変えたら Stripe 側も同じ文言に直すこと。** */
export const reserveQuestions = [
  '今、どんな状態ですか。ひとことで構いません',
  'これから、どうありたいですか',
  '気になっている色があれば教えてください（なければ「おまかせ」で）',
] as const;

/** ── 次の回を出す手順 ──
 *
 *  ★2026-08-08：今回は**テスト的な販売**ですが、うまくいけば同じ形で続ける前提です（岡さん）。
 *    だから毎回ページを作り直さなくて済むようにしてあります。
 *    **続けるときに触るのは、このファイルと写真の追加だけ**です。
 *
 *  1. 次に出すものを並べて、1枚撮る（前回と同じ竹ざる・真俯瞰だと並びが揃います）
 *  2. 幅1600pxのJPEGに書き出して `public/assets/ishi/vol02.jpg` として**新規に置く**
 *     🔴 **上書きしないこと。** 過去の回の写真は「これまでのお見立て」で使い続けます
 *     ⚠️ 元データは _Inbox に残すこと。書き出し済みのものを再圧縮しない
 *  3. 上の volumes 配列の**末尾に1つ足す**（vol / photo / alt / count / released / w / h）
 *     ⚠️ w / h は書き出した実寸を入れること
 *  4. open を true に戻す（残数はサイトで持っていません。Stripe側の上限が在庫です）
 *  5. Stripeで新しい決済リンクを作り（下の設定メモ参照）、stripeLink を差し替える
 *     ⚠️ 前回のリンクを使い回さないこと。購入回数の上限が前回のぶんのまま残っています
 *  6. `npm run build` が通ることを確認して、ローカルで /mind/ を見る
 *     → 前回のぶんが「これまでのお見立て」に自動で移っていれば成功です
 *  7. LINEで告知（前回の売り切れ時に「次はLINEで」と案内しているので、ここまでが1セットです）
 *
 *  ⚠️ 岡さんの原稿（/mind/ の③④）は毎回書き換えるものではありません。中身が変わっても
 *     「守ってもらうものではない → 共鳴装置」の話は同じなので、そのままにしてください。 */

/** ── Stripe側の設定メモ（決済リンクを作る人へ）──
 *
 *  1. 商品名は「岡のお見立て Vol.01（パワーストーン）」。
 *     **選べないことが分かる名前**にしてください。「タイガーアイ」など石の名前は使わないこと。
 *     ⚠️ 決済画面に出る名前が、サイトの名前と違うと不安を与えます。SERVICE_NAME と揃えること。
 *     ⚠️ Vol番号を入れておくと、回を重ねたときにダッシュボードで見分けられます。
 *  2. 商品説明はこの2行。**「お選びいただけません」を決済画面にも必ず出すこと。**
 *     メールのリンクから直接来た人は、サイトを読んでいません。読むのはこの画面だけです。
 *       お申し込み時のお答えをもとに、岡慶行が一点を選んでお届けします。
 *       天然石の一点ものです。石はお選びいただけません。送料込み。
 *  3. 金額 18,000円（税込・送料込）。
 *  4. 「購入回数の上限」を **その回の count と同じ数**（Vol.01 なら 9）に設定してください。
 *     これを忘れると、9つしかないのに10件目が決済できてしまいます。
 *  5. カスタムフィールドに、上の reserveQuestions の3つを**同じ文言で**入れてください。
 *     岡さんはここに入った回答を見て選びます。
 *  6. 配送先の住所入力を**必須**にしてください（物販なので）。
 *  7. カード明細に出る表記（statement descriptor）を確認してください。
 *     意味不明な英数字だと「身に覚えのない請求」と思われ、問い合わせになります。
 *
 *  ⚠️ 在庫はStripeの上限だけで管理します。サイトは残数を持っていません。
 *     ただし**売り切れてもサイトのボタンは押せたまま**です（押すと無効リンクの画面に飛びます）。
 *     売り切れたら open を false にしてください。 */
export const STRIPE_SETUP = true;
