/** よく読まれているはなし（ピックアップ）
 *
 *  ここに書いた順番が、そのままサイトの並び順になります。
 *  **記事のファイル名（.md を除いた部分）を、上から見せたい順に並べてください。**
 *
 *  ── 使い方 ──────────────────────────────
 *  ・入れ替えるときは、この配列を書き換えるだけです。記事側は触りません
 *  ・空の配列 `[]` にすると、ブロックごと消えます（記事が少ないうちはそれでOK）
 *  ・存在しない・下書きのファイル名を書いても、無視されるだけで壊れません
 *  ・トップページの「書いてきたこと」4本も、ここが優先されます
 *    （足りない分は新しい記事で埋まります）
 *
 *  ── なぜ順位を実測にしていないのか（2026-08-07） ─────────
 *  🔴 サイトにまだ計測タグ（GA4）が入っていません。実際のPVが取れないので、
 *     いまは**編集判断で選んだピックアップ**です。「ランキング」とは名乗りません。
 *  記事が9本しかない段階で順位を出しても、ほぼ偶然で決まってしまうためでもあります。
 *
 *  ── 実測に切り替えるとき（Phase 3）────────────────
 *  ⚠️ このファイルの**形（slugの配列）を変えないでください**。
 *     GA4 Data API → GASが月1で slug の並びを書き出す、という形に差し替える前提です。
 *     配列の中身が入れ替わるだけなら、表示側は一切直さずに済みます。
 *  手順の正本は `30_Content/BLOG_RULES.md`。
 *
 *  📌 いまの3本は**仮です**（福田が内容から選んだもの）。差し替えてください。
 */
export const popularSlugs: string[] = [
  'toilet-and-money-luck',
  'what-is-luck',
  'are-you-human',
];

/** ブロックの見出し。実測に切り替えたら「よく読まれているはなし」に変えてください。
 *  🔴 実測でないうちに「ランキング」「人気1位」と書かないこと。嘘になります。 */
export const popularHeading = 'はじめて読むなら';
